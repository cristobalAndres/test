import { TestBed, inject } from '@angular/core/testing';

import { InitialBalanceImportService } from './import.service';
/*
describe('InitialBalanceImportService', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [InitialBalanceImportService]
    });
  });

  it('should ...', inject([InitialBalanceImportService], (service: InitialBalanceImportService) => {
    expect(service).toBeTruthy();
  }));
});
*/
