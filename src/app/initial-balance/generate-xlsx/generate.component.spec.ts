import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { InitialBalanceGenerateXLSXComponent } from './generate.component';
/*
describe('LogComponent', () => {
  let component: InitialBalanceGenerateXLSXComponent;
  let fixture: ComponentFixture<InitialBalanceGenerateXLSXComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [InitialBalanceGenerateXLSXComponent]
    })
      .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(InitialBalanceGenerateXLSXComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should be created', () => {
    expect(component).toBeTruthy();
  });
});
*/
