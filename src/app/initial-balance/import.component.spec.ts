import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { InitialBalanceImportComponent } from './import.component';
/*
describe('InitialBalanceImportComponent', () => {
  let component: InitialBalanceImportComponent;
  let fixture: ComponentFixture<InitialBalanceImportComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ InitialBalanceImportComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(InitialBalanceImportComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/
