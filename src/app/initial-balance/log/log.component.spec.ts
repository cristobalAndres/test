import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { InitialBalanceImportLogComponent } from './log.component';
/*
describe('LogComponent', () => {
  let component: InitialBalanceImportLogComponent;
  let fixture: ComponentFixture<InitialBalanceImportLogComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [InitialBalanceImportLogComponent]
    })
      .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(InitialBalanceImportLogComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should be created', () => {
    expect(component).toBeTruthy();
  });
});
*/
