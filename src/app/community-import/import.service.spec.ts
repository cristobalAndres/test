import { TestBed, inject } from '@angular/core/testing';
import { CommunityImportService } from './import.service';
/*
describe('CommunityImportService', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [CommunityImportService]
    });
  });

  it('should ...', inject([CommunityImportService], (service: CommunityImportService) => {
    expect(service).toBeTruthy();
  }));

  it('should return observable', inject([CommunityImportService], (service: CommunityImportService) => {
  }));
});
*/
